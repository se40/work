入力部分のwhileでハッシュの初期化、ハッシュをpushする箇所を独自に
実装してみたがうまくいっておらず、データ構造がフラットになっていた。
また、データには空行があり、空行の処理も失敗していた。

出力部分で、「Use of uninitialized value in print at ./packet_log_edit.pl」
というエラーが出ていたのは入力部分でデータ構造がフラットになっていたから。

Dumperでの内部データの確認により、期待する出力が出せた。